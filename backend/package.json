{
  "name": "notigram-backend",
  "version": "0.1.0-beta",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init": "wrangler d1 execute notigram-db --local --file=./schema.sql",
    "d1:init:prod": "wrangler d1 execute notigram-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240903.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.78.0"
  }
}